spring:
  application:
    name: smart-shop
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: true
      format_sql: true
    properties:
        hibernate:

            jdbc:
              lob:
                non_contextual_creation: true
            temp:
                use_jdbc_metadata_defaults: false

  datasource:
    url: jdbc:postgresql://ep-polished-flower-a1hcns4i-pooler.ap-southeast-1.aws.neon.tech:5432/smart_link?sslmode=require
    username: neondb_owner
    password: npg_xWZ4N8SnXHkl
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connectionTimeout: 30000
      idleTimeout: 600000
      maxLifetime: 1800000

  mvc:
    static-path-pattern: /static/**
    web:
      resources:
        static-locations: classpath:/static/,classpath:/public/

# Logging configuration
logging:
  config: classpath:logback-spring.xml
  level:
    root: INFO
    com.menghor.smart_shop: INFO
    org.springframework: INFO
    org.hibernate: WARN


server:
  port: 8080

jwt:
  secret:
    key: da2bd42bef8f9f957d647ed13571d35b59c7055fb723eacdb05154878da17d77d3180385fc232cab38bded0fc7dbda0d439cc275e91164ade266d64d50633bc7
  expiration-min: 9999999999999

springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
    enabled: true
    url: /v3/api-docs
    config-url: /v3/api-docs/swagger-config
  api-docs:
    path: /v3/api-docs
    enabled: true
  packagesToScan: com.menghor.smart_shop
